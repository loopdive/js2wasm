---
priority: high
test262_skip: 1105
feasibility: medium
status: review
completed: 2026-03-19
files:
  tests/test262-runner.ts:
    new:
      - "asyncHelpers.js include + asyncTest/throwsAsync shims"
      - "tcoHelper.js include + $MAX_ITERATIONS shim"
      - "deepEqual.js include allowed (no shim needed)"
      - "compareIterator.js include allowed (no shim needed)"
      - "assert.throwsAsync -> assert.throws transform"
    breaking: []
depends_on: [125]
---
# #606 — Test harness gaps: propertyHelper.js, assert.throws, String() indexer (1,105 tests)

## Status: review

Three test harness issues block 1,105 tests:

| Filter | Tests | Issue |
|--------|------:|-------|
| unsupported include: propertyHelper.js | 647 | #129 — needs verifyProperty stubs |
| assert.throws with side-effect assertions | 342 | Runner doesn't support assert.throws with post-throw checks |
| String() indexer in assert | 116 | String used as indexer in assert harness code |

## Approach

1. **propertyHelper.js**: Already implemented (in allowed set + shims for verifyProperty et al)
2. **assert.throws**: Already handled via transformAssertThrows
3. **String() indexer**: Already handled (skip was removed)

## Additional improvements (this PR)

4. **asyncHelpers.js** (130+ tests): Added to allowed includes. Shims `asyncTest()` and transforms `assert.throwsAsync()` -> `assert.throws()` -> `assert_throws()`.
5. **tcoHelper.js** (34 tests): Added to allowed includes. Shims `$MAX_ITERATIONS = 100000`.
6. **deepEqual.js** and **compareIterator.js**: Added to allowed includes (small number of tests).

## Complexity: M

## Implementation Summary

### What was done
- Added `asyncHelpers.js`, `tcoHelper.js`, `deepEqual.js`, `compareIterator.js` to the allowed includes set
- Added `asyncTest()` shim for asyncHelpers.js (calls test function synchronously with try/catch)
- Added `$MAX_ITERATIONS` declaration for tcoHelper.js
- Added `assert.throwsAsync()` -> `assert.throws()` text transform (reuses existing transformAssertThrows)
- No changes needed for propertyHelper.js, assert.throws, or String() indexer (already implemented)

### Files changed
- `tests/test262-runner.ts`: Extended allowed includes set, added shims and transforms

### Results
- 0 skips on `language/statements/try` (previously had tcoHelper skips)
- 0 skips on `built-ins/Math/abs` (propertyHelper tests pass)
- asyncTest and $MAX_ITERATIONS shims verified via unit test
