---
id: 1064
title: "DataView bridge: propagate RangeError / TypeError from native getX/setX calls"
sprint: Backlog
priority: medium
feasibility: easy
reasoning_effort: low
depends_on: []
created: 2026-04-11
updated: 2026-04-11
es_edition: multi
language_feature: dataview
task_type: bug
---

# #1064 — DataView bridge: propagate RangeError / TypeError from native getX/setX calls

## Status: Reverted 2026-04-11 — awaiting reapply + interaction bisect

**Original fix**: PR #107 (merge commit `fc4b06c8`, branch
`issue-1064-dataview-errors`, shipped 2026-04-11 19:14 UTC). dev-1053
rewrote the issue from the original "wrap in try/rethrow" sketch to a
sidecar model: track `byteOffset` / `byteLength` per DataView instance
so the runtime bridge reconstructs the correct subview with proper
bounds checking. 6/6 local tests pass + 11/11 `get*/index-is-out-of-range`
test262 cases flip from fail → pass.

**Why it was reverted**: PR #107 was part of the 3-PR interaction (#96 +
#100 + #107) that caused the 2026-04-11 CI baseline flip. Individual
revert of #107 alone (PR #113) did NOT recover the baseline — still
at 20,599. Only the combined 3-PR revert (PR #114) restored the healthy
22,157 baseline.

**Reapply sequence**: dev-1053 opened **PR #116** on 2026-04-11 ~21:00 UTC
cherry-picking commit `38afe7d1` onto post-rescue main (`65ea04b5`).
PR #116 is the empirical probe to test whether #107 alone, on a clean
baseline, is sufficient to trigger the flip. Expected outcomes documented
in the PR body.

**Full context**:
- `plan/investigations/2026-04-11-baseline-regression-bisect.md`
- `plan/retrospectives/2026-04-11-ci-baseline-drift-investigation.md`

**Work to redo if PR #116 is rejected**: restore the 5-file patch from
`git show 38afe7d1` (`src/codegen/expressions/new-super.ts`,
`src/runtime.ts`, `plan/issues/ready/1064.md`, `tests/issue-1064.test.ts`,
plus the issue-file update) once the interaction is isolated and a
forward-fix is in place.

## Problem

Follow-up from #1056 (PR #90). The new DataView bridge in `src/runtime.ts` materializes a real
`DataView` over the `i32_byte` vec struct and calls the native `getUint16` / `setFloat32` / etc.
methods. When the native method throws (e.g. `RangeError` for out-of-range offset, `TypeError`
for detached buffer), the throw is either swallowed or returned as a value, so the test's
`assert.throws(RangeError, ...)` assertion fails.

## Evidence

PR #90 branch CI (`issue-1056-dataview-setters`) against post-#1056-merge baseline:

- **+76 improvements** (net-positive fix landed)
- **16 regressions**, 15 of which are the same bug class:

```
  11× DataView/prototype/get{Int8,Int16,Int32,Uint8,Uint16,Uint32,Float16,Float32,Float64,BigInt64,BigUint64}/index-is-out-of-range.js
   4× DataView/prototype/{getInt8,getUint8,setInt8,setUint8}/detached-buffer.js
   1× ArrayBuffer/prototype/slice/species.js  (unrelated, already-failing orthogonal test)
```

Sample failure message:

```
returned 6 — assert #5 at L48: assert.throws(RangeError, function() {
  sample.getUint16(1);
}, "1 + 2 > 2 (offset)");
```

The test calls `assert.throws(RangeError, fn)`. The runtime bridge executes `fn()` without a
try/catch around the native method invocation, so either:

- The throw is swallowed (bridge returns default), or
- The throw surfaces as a non-`RangeError` externref, failing `assert.throws`'s constructor check.

## Fix

In `src/runtime.ts` — `__extern_method_call` DataView branch (the one that matches
`/^(get|set)(Uint|Int|Float|BigInt|BigUint)(8|16|32|64)$/`):

1. Wrap the native `(get|set)Xxx()` call in `try { ... } catch (e) { throw e; }` so the throw
   propagates back through the externref boundary as a real error.
2. For `set*` methods, write bytes back via `__dv_byte_set` ONLY on success — do not write
   partial bytes if the native throws.
3. Verify the caller path converts the thrown externref back to a catchable Wasm exception
   (should already work via the standard Wasm exception bridge — confirm).

## Expected impact

~15 FAIL → PASS in `test/built-ins/DataView/prototype/*/index-is-out-of-range.js` and
`detached-buffer.js` buckets.

## Source

Filed by `dev-1056` during self-merge triage of PR #90 (#1056). 16 regressions were within
self-merge narrow-cluster criteria; filing this as the follow-up per skill protocol.
