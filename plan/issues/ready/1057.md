---
id: 1057
title: "String.prototype.split result constructor !== Array"
sprint: Backlog
status: ready
priority: low
feasibility: medium
reasoning_effort: medium
depends_on: []
created: 2026-04-11
updated: 2026-04-11
es_edition: multi
language_feature: test262-harvest-cluster
task_type: bug
---

# #1057 — String.prototype.split result constructor !== Array

## Status: Partial fix reverted 2026-04-11 — awaiting reapply + interaction bisect

The runtime half of #1057 shipped as **PR #100** (merge commit `2dc095ac`),
adding a `__extern_get` constructor short-circuit that returns the real
`Array` ctor for vec-struct wrappers. The compiler half was tracked as
follow-up #1065 (Array-as-declared-global registration).

**PR #100 was reverted as part of PR #114** (#96 + #100 + #107 combined
revert) on 2026-04-11 20:56 UTC after individual reverts of #96 and #107
failed to recover main's CI baseline. Only the combined revert restored
the pre-flip state of 22,157 pass. Neither #100 alone nor any single PR
is the root cause — the bug is a 2-of-3 or 3-of-3 interaction.

**Reapply sequence in flight**: dev-1053 opened PR #116 reapplying #107
first; #100 follows. Details in `plan/investigations/2026-04-11-baseline-regression-bisect.md`
and `plan/retrospectives/2026-04-11-ci-baseline-drift-investigation.md`.

**Work to redo on reapply**: restore the 9-LOC `__extern_get` short-circuit
in `src/runtime.ts` from `git show 2dc095ac`. Follow-up #1065 (compiler
half) tracks the remaining part.

## Problem

`String.prototype.split` returns an array-like whose `.constructor` is not `Array`. Tests assert `__split.constructor === Array` and fail.

## Evidence from harvest

- **Test count:** 68 tests currently failing with this pattern
- **Top path buckets:**
  - `68 test/built-ins/String/prototype/split/*`
- **Top error messages:**
  - 24× `returned 2 — assert #1 at L18: assert.sameValue(__split.constructor, Array,`
- **Sample test files:**
  - `test/built-ins/String/prototype/split/argument-is-regexp-d-and-instance-is-string-dfe23iu-34-65.js`
  - `test/built-ins/String/prototype/split/arguments-are-new-reg-exp-and-void-0-and-instance-is-string-hello.js`
  - `test/built-ins/String/prototype/split/call-split-l-3-instance-is-string-hello.js`

## Root cause hypothesis

Our split implementation returns a WasmGC string[] ref instead of a proper JS Array. The prototype chain on the returned value does not point at `Array.prototype`.

## Fix

Ensure `split` materializes a true JS Array (goes through the same Array allocation path as `[]`/`Array.of`) so that `.constructor` resolves to `Array`. Alternatively, install `constructor === Array` on the string-array result's prototype surface.

## Expected impact

~68 FAIL, plus likely unblocks any downstream test that iterates `split` output via Array-specific methods.

## Key files

- src/codegen/expressions.ts (String.split host import / lowering)
- runtime split builtin

## Source

Filed by `harvester-post-sprint-40-merge` 2026-04-11 against the post-merge Sprint 40 main baseline (`benchmarks/results/test262-current.jsonl`, 43,164 records).
