---
priority: critical
test262_ce: 4443
feasibility: medium
depends_on: []
---
# #765 -- Compile error triage: 4,443 remaining CEs by pattern

## Status: ready

## Problem

4,443 tests fail at compile time (WebAssembly validation errors). Previous issues (#409, #411, #444, #511, #515) addressed major CE categories but are now in done/review status. The remaining CEs need fresh triage to identify current top patterns.

### Known remaining patterns (from latest report)

| Pattern | Est. Count | Related Issue |
|---------|-----------|---------------|
| `call[N] expected externref, found ref` | ~1,299 | #759 (ready) |
| `struct.new stack mismatch` | ~500 | #411 (done, residual) |
| `local.set type mismatch` | ~400 | #444 (done, residual) |
| `immutable global cannot be assigned` | ~200 | strict mode directives |
| `expected 0 elements on stack for fallthru` | ~150 | control flow |
| Other validation errors | ~1,894 | unclassified |

### What needs to happen

1. Run test262 with `--resume` and collect all CE error messages
2. Group by error pattern (regex on stderr)
3. Create sub-issues for each pattern with >50 occurrences
4. Prioritize by count (highest first)

### Relation to existing issues

- #759 (extern.convert_any insertion) covers 1,299 of these
- Fixing #759 alone would reduce CEs by ~29%
- This issue tracks the umbrella triage for the remaining ~3,144

## Complexity: S (triage) → spawns M-L sub-issues

## Acceptance criteria

- All 4,443 CEs classified by error pattern
- Sub-issues created for each pattern with >50 occurrences
- Priority ordering based on count
