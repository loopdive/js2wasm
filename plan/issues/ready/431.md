---
id: 431
title: "Math.pow/min/max conditional expressions produce fallthru type mismatch (27 CE)"
status: ready
priority: medium
depends_on: []
files:
  src/codegen/expressions.ts:
    new: []
    breaking:
      - "compileConditionalExpression — result type unification when branches produce different Wasm types"
      - "compileBinaryExpression — coerce f64 to externref when needed in ternary/if context"
---
# #431 — Math.pow/min/max conditional expressions produce fallthru type mismatch (27 CE)

## Problem

27 tests fail with:

```
type error in fallthru[0] (expected externref, got f64)
```

These are all in Math.pow (21), Math.min (2), Math.max (2), and Math.hypot (2) categories. The tests use conditional expressions or if/else blocks where one branch produces an f64 (from a Math call) and the surrounding context expects externref, or the two branches of a conditional produce different types.

### Sample failing tests

- `test/built-ins/Math/pow/applying-the-exp-operator_A1.js` through `_A16.js` (21 tests)
- `test/built-ins/Math/min/S15.8.2.12_A2.js`
- `test/built-ins/Math/max/S15.8.2.11_A2.js`

### Sample test pattern (Math.pow A1)

```javascript
var base = new Array();
base[0] = -Infinity;
// ...
for (var i = 0; i < basenum; i++) {
  assert.sameValue(Math.pow(base[i], exponent), NaN, base[i]);
}
```

The issue is that `Math.pow` returns f64, but the test harness `assert.sameValue` expects externref arguments. The compiler fails to insert the coercion from f64 to externref at the call boundary.

## Root cause

When a conditional expression (ternary or if/else) has branches that produce different Wasm types (e.g., one branch f64, another externref), the Wasm `if` block requires both branches to have the same result type. The compiler does not unify the branch types by inserting coercions.

Additionally, when f64 values are passed to functions expecting externref parameters, the compiler sometimes fails to emit `__box_number` coercion, particularly in complex expression contexts involving arrays, loops, and function calls.

## Priority: medium (27 tests)

## Complexity: S

## Acceptance criteria
- [ ] Math.pow tests compile without fallthru type errors
- [ ] f64 results properly coerced to externref when passed to externref-typed parameters
- [ ] Conditional expressions with mixed-type branches get proper type unification
- [ ] Reduce "type error in fallthru" CEs to zero
