---
priority: high
depends_on: []
files:
  src/codegen/statements.ts:
    new: []
    breaking:
      - "compileStatement: extend ClassDeclaration handling to work inside generator, arrow, and async function bodies"
  src/codegen/index.ts:
    new: []
    breaking:
      - "compileClassBodies: support class declarations in nested function scopes"
---
# Issue #234: ClassDeclaration in nested/expression positions (remaining 681 errors)

## Status: in-progress

## Summary

681 tests fail with "Unsupported statement kind: ClassDeclaration" despite Sprint 2's #220 fixing ClassDeclaration in statement positions. The remaining errors occur in contexts where class declarations appear inside other constructs that were not covered: inside generator functions, inside arrow functions, or combined with other unsupported patterns.

## Root Cause

Sprint 2's #220 added ClassDeclaration support in if-blocks, for-loops, and top-level statements. But many test262 tests use class declarations inside:
1. Generator function bodies (`function* gen() { class C {} }`)
2. Arrow function bodies
3. Combined with `yield` expressions (which cause separate errors)
4. Test patterns where the class is the primary subject but appears inside a wrapper function

Of the 681 errors, ~195 combine with "Unsupported call expression" and ~89 combine with "Unsupported new expression for class: C", suggesting the class compiles but its instantiation or method calls fail.

## Scope

- `src/codegen/statements.ts` -- ClassDeclaration handling in nested function contexts
- Tests affected: ~681 compile errors (though many are combined with other errors)

## Expected Impact

Fixing the pure ClassDeclaration cases (where it is the only error) could resolve ~200-300 compile errors. Cases combined with other errors will need those errors fixed too.

## Suggested Approach

1. Audit which ClassDeclaration errors are sole errors vs combined with other errors
2. For sole errors, trace why the class fails to compile in the specific context
3. Ensure ClassDeclaration works inside all function body types (regular, arrow, generator, async)
4. Handle `new C()` for locally-declared classes (the "Unsupported new expression for class: C" pattern)

## Acceptance Criteria

- [ ] ClassDeclaration compiles in generator function bodies
- [ ] ClassDeclaration compiles in arrow function bodies
- [ ] `new C()` works for locally-declared classes
- [ ] At least 150 compile errors resolved

## Complexity: L
