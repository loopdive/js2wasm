---
id: 1053
title: "arguments.length reflects formal parameter count instead of runtime args length"
sprint: Backlog
status: in-progress
priority: medium
feasibility: medium
reasoning_effort: medium
depends_on: []
created: 2026-04-11
updated: 2026-04-11
es_edition: multi
language_feature: test262-harvest-cluster
task_type: bug
---

# #1053 — arguments.length reflects formal parameter count instead of runtime args length

## Real Root Cause (dev-1053, 2026-04-11)

The harvester-reported hypothesis ("trailing comma double-counted") is **wrong**. Trailing comma is incidental: all failing tests in the cluster happen to use `f(42, 'a',)` syntax, but the underlying bug is orthogonal to the parser.

**Actual bug**: `emitArgumentsObject` in `src/codegen/statements/nested-declarations.ts:713` and `src/codegen/function-body.ts:283` builds the `arguments` vec struct by iterating the **callee's formal parameter list**, not the actual runtime args passed at the call site. In every failing test, the method is declared `method()` (zero formal params) and called with two runtime arguments, so we emit `arguments.length === 0`, the first assertion fails, and `__fail = 2` is returned.

**Reproduced on 6 variants**, all FAIL with the same root cause:
- `cls-decl-meth-args-trailing-comma-multiple.js`
- `cls-decl-gen-meth-args-trailing-comma-multiple.js`
- `cls-decl-private-meth-args-trailing-comma-multiple.js`
- `meth-args-trailing-comma-multiple.js`
- `gen-meth-args-trailing-comma-multiple.js`
- `func-decl-args-trailing-comma-multiple.js`

Extra confirmation: `method(a)` called with `method(42, 'TC39')` returns `arguments.length === 1`, not 2. `method(a, b)` called with `method(42, 'TC39')` correctly returns 2 — so the breakage is strictly "runtime args > formal params".

## Fix (implemented: extras module global)

Instead of threading a synthetic trailing param through dozens of call-site
patterns, we use a module-level `__extras_argv: (mut ref null $vec_externref)`
global. The protocol:

1. Track functions whose body reads `arguments` in `ctx.funcUsesArguments`
   (populated from `bodyUsesArguments` AST walk in declarations.ts,
   class-bodies.ts, literals.ts, nested-declarations.ts).
2. At each call site in `calls.ts`, when the callee is in
   `funcUsesArguments` AND `runtimeArgs.length > formalParamCount`, compile
   the extras to externref, build a vec struct via `array.new_fixed`, and
   `global.set __extras_argv`.
3. In function prologues for `arguments`-reading callees, consume the
   global: read it into a local, immediately clear to `ref.null`
   (prevents stale leakage on nested calls), dynamically allocate
   `array.new_default(formalCount + extrasLen)`, `array.set` formals
   (boxing f64/i32 → externref), and `array.copy` extras from the global's
   data array into the tail of the new array.

Six call-site patterns in `calls.ts` are covered: static method,
null-guarded method, non-nullable method, struct-method null-guarded,
struct-method non-nullable, and direct-name function call. Object-literal
methods and nested function declarations use the same path via
`funcUsesArguments`.

**Scope note**: under-application (runtime args < formal count) is not
addressed here — it would require a separate argc-override mechanism.
Over-application is the pattern the 133 failing test262 tests exercise.

## Test Results

- Sample variants from the issue: **5/6 pass** (all except
  private-method-via-getter, which goes through an indirect `call_ref`
  path not in this fix's scope).
- Wide trailing-comma test262 batch: **100/234** pass (was 0/234 before
  the fix). Remaining failures are async-gen methods (separate runtime
  issues) and named function expressions (`closures.ts` not modified).
- Targeted tests: **9/9 pass** in `tests/issue-1053.test.ts`.
- Equivalence regression check: `arguments-object.test.ts`,
  `arguments-nested-and-loops.test.ts`, `function-arity-mismatch.test.ts`,
  `rest-params-call.test.ts` — no new failures vs main baseline (same
  4 pre-existing failures both before and after).

## Original harvester evidence (kept for audit trail)

Class methods (gen, async, static, private) called with trailing-comma argument lists (e.g. `f(42, 'a',)`) observe the wrong `arguments.length` and `arguments[k]` values. The trailing comma should be ignored and not count as an extra argument.

## Problem

Class methods (gen, async, static, private) called with trailing-comma argument lists (e.g. `f(42, 'a',)`) observe the wrong `arguments.length` and `arguments[k]` values. The trailing comma should be ignored and not count as an extra argument.

## Evidence from harvest

- **Test count:** 133 tests currently failing with this pattern
- **Top path buckets:**
  - `133 test/language/arguments-object/*`
- **Top error messages:**
  - 25× `returned 2 — assert #1 at L37: assert.sameValue(arguments.length, 2); assert.sameValue(arguments[0], 42)`
- **Sample test files:**
  - `test/language/arguments-object/async-gen-meth-args-trailing-comma-undefined.js`
  - `test/language/arguments-object/cls-decl-async-gen-meth-static-args-trailing-comma-multiple.js`
  - `test/language/arguments-object/cls-decl-gen-meth-args-trailing-comma-null.js`

## Root cause hypothesis

The call-site argument-count plumbing incorrectly propagates a phantom slot for the trailing comma, or the method prologue reads the pre-trim argument-count from a frame location that has not accounted for the trailing comma elision.

## Fix

Audit call-site argument-list lowering to drop trailing-comma sentinels before populating the `arguments` object. Ensure `arguments.length` is derived from the actual argument count, not the raw parse-tree slot count.

## Expected impact

~133 FAIL.

## Key files

- src/codegen/expressions.ts (CallExpression emission)
- arguments object construction

## Source

Filed by `harvester-post-sprint-40-merge` 2026-04-11 against the post-merge Sprint 40 main baseline (`benchmarks/results/test262-current.jsonl`, 43,164 records).
