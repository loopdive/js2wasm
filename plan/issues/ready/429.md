---
id: 429
title: "Undeclared variable access should produce ReferenceError (71 tests)"
status: ready
priority: high
depends_on: []
files:
  src/codegen/expressions.ts:
    new: []
    breaking:
      - "compileIdentifier — undeclared variable handling"
      - "global variable emission — mutable vs immutable"
  src/codegen/index.ts:
    new: []
    breaking:
      - "global declaration — emit mutable globals for undeclared variables"
---
# #429 — Undeclared variable access should produce ReferenceError (71 tests)

## Problem

71 tests across 15+ expression categories fail because the compiler does not handle undeclared variables correctly. Two distinct sub-problems:

### Sub-problem 1: Immutable global assignment (37 CE)

When a test does `x = 1` where `x` was previously undeclared, the compiler creates an immutable global for `x` but then tries to assign to it, producing:

```
immutable global #N cannot be assigned
```

This affects tests like `S11.9.1_A2.1_T2.js` (equals), `S11.4.9_A2.1_T2.js` (logical-not), and many more across all expression categories.

### Sub-problem 2: Missing ReferenceError for undeclared reads (34 fail)

Tests like `x + 1` where `x` is never declared expect a ReferenceError to be thrown. The compiler instead silently treats `x` as a global with a default value, so no error is thrown and the test fails with "returned 0".

Pattern: `_A2.1_T2` and `_A2.1_T3` tests across addition, subtraction, multiplication, division, modulus, bitwise-and/or/xor, shifts, comparison, equality, logical-not.

### Sample failing tests

- `test/language/expressions/addition/S11.6.1_A2.1_T2.js`
- `test/language/expressions/equals/S11.9.1_A2.1_T2.js`
- `test/language/expressions/logical-not/S11.4.9_A2.1_T2.js`

## Root cause

The compiler does not distinguish between:
1. Variables declared with `var`/`let`/`const` (which should be globals)
2. Variables that are never declared (which should throw ReferenceError on access)

For sub-problem 1, globals created for undeclared variables should be mutable if they may be assigned.
For sub-problem 2, accessing a truly undeclared variable should emit a trap or ReferenceError equivalent.

## Priority: high (71 tests across 15+ categories)

## Complexity: M

## Acceptance criteria
- [ ] Globals created for assignable variables are mutable
- [ ] Undeclared variable reads throw ReferenceError (or trap)
- [ ] Reduce "immutable global cannot be assigned" CEs to zero
- [ ] A2.1_T2/T3 pattern tests pass across expression categories
