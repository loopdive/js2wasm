---
priority: high
test262_fail: 988
feasibility: medium
files:
  src/codegen/expressions.ts:
    breaking:
      - "use unified closure struct or ref.test for cross-scope closures"
depends_on: [585]
---
# #648 — Residual illegal cast (988 FAIL)

## Status: open

988 tests fail with "RuntimeError: illegal cast". #585 fixed closure return type mismatch (44/51). Remaining cases involve:
- Generator + destructuring patterns (different code path)
- Closures passed through module globals (no contextual type at creation)
- Cross-scope closure struct mismatches

### Fix
Use ref.test before ref.cast for closure calls where the struct type isn't guaranteed. Fall back to externref dispatch on type mismatch.

## Complexity: M
