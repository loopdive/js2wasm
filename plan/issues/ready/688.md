---
priority: medium
feasibility: hard
files:
  src/codegen/expressions.ts:
    breaking:
      - "split 27K line file into modules per language feature"
depends_on: []
---
# #688 — Refactor codebase into smaller modules per language feature

## Status: in-progress

expressions.ts is 27K lines. Split into ~10 modules per language feature.

## Split plan (in order of safety/independence)

| # | New File | ~Lines | Key Functions | Risk |
|---|----------|--------|---------------|------|
| 1 | `array-methods.ts` | 3,500 | compileArray*, setupArrayCallback, buildClosureCallInstrs | Low — self-contained |
| 2 | `string-ops.ts` | 1,500 | compileStringLiteral, compileTemplate*, compileNativeStringMethodCall | Low |
| 3 | `binary-ops.ts` | 2,000 | compileBinaryExpression, compileNumericBinaryOp, compileBitwiseBinaryOp | Low |
| 4 | `generators.ts` | 500 | compileYieldExpression, generator helpers | Low |
| 5 | `typeof-delete.ts` | 800 | compileTypeofExpression, compileDeleteExpression, compileInstanceOf | Low |
| 6 | `closures.ts` | 2,500 | compileArrowFunction, compileArrowAsClosure, compileArrowAsCallback | Medium — many shared deps |
| 7 | `object-ops.ts` | 1,500 | compileObjectDefineProperty, compileObjectKeysOrValues | Medium |
| 8 | `property-access.ts` | 2,500 | compilePropertyAccess, emitNullGuardedStructGet, emitExternrefToStructGet | HIGH — most actively edited |
| 9 | `calls.ts` | 3,000 | Call dispatch, method calls, .call/.apply, super, new | HIGH — many code paths |
| 10 | `expressions.ts` | 4,000 | Entry point: compileExpression, switch dispatcher, identifiers, literals, unary, assignment | Stays |

## Principles
- **No behavior change** — pure refactor, same exports
- **One module per PR** — easier to review, catch regressions
- **Tests must pass after each split**
- **Do NOT split while agents are editing the same functions** — check worktree diffs first
- **compileExpressionInner stays in expressions.ts** — it's the switch dispatcher

## Execution order
Start with #1 (array-methods) — safest, biggest, most self-contained. Do high-risk splits (#8, #9) last when no agents are active.

## Complexity: XL

## Step 1: array-methods.ts -- DONE

Extracted ~3,100 lines of array method code from `expressions.ts` into `src/codegen/array-methods.ts`.

### What was extracted
- `resolveArrayInfo`, `getReceiverLocalIdx`, `compileArrayPrototypeCall`, all `compileArrayPrototype*` functions
- `compileArrayMethodCall`, `emitArrayCopy`, `compileArrayAt`, `compileArrayIndexOf`, `compileArrayIncludes`, `compileArrayReverse`, `compileArrayPush`, `compileArrayPop`, `compileArrayShift`, `compileArraySlice`, `compileArrayConcat`, `compileArrayJoin`, `compileArraySplice`
- Functional array methods: `setupArrayCallback`, `setupArrayLoop`, `buildClosureCallInstrs`, `buildBridgeCallInstrs`, `buildTruthyCheck`, `buildFalsyCheck`, `emitArrayLoop`, `loopExitCheck`, `loopIncrement`, `buildCallAndCheck`
- `compileArrayFilter`, `compileArrayMap`, `compileArrayReduce`, `compileArrayForEach`, `compileArrayFind`, `compileArrayFindIndex`, `compileArraySome`, `compileArrayEvery`, `compileArraySort`, `compileArrayFill`, `compileArrayCopyWithin`, `compileArrayLastIndexOf`
- `ARRAY_METHODS` set, `ArrayCallbackSetup` and `ArrayLoopLocals` interfaces

### Exports added to expressions.ts (for array-methods.ts to import)
- `VOID_RESULT`, `getLine`, `getCol`, `emitClampIndex`, `emitClampNonNeg`, `compileArrowAsClosure`

### Verification
- equivalence tests: 1059 pass / 38 fail (identical to baseline)
- no new TypeScript errors (all errors in array-methods.ts are pre-existing type mismatches moved from expressions.ts)
- `expressions.ts`: 27,191 -> 24,090 lines (-3,101 lines)

## Step 8: property-access.ts -- IN PROGRESS

Extracting property access code from `expressions.ts` into `src/codegen/property-access.ts`.

### What was extracted
- Null guard helpers: `typeErrorThrowInstrs`, `emitNullCheckThrow`, `findAlternateStructsForField`, `emitNullGuardedStructGet`, `emitExternrefToStructGet`
- Property access: `compilePropertyAccess`, `compileExternPropertyGet`
- Bounds-checked array access: `emitBoundsCheckedArrayGet`, `emitClampIndex`, `emitClampNonNeg`, `emitBoundsGuardedArraySet`, `isSafeBoundsEliminated`
- Element access: `compileElementAccess`, `compileElementAccessBody`

### Functions newly exported from expressions.ts (for property-access.ts to import)
- `ensureLateImport`, `flushLateImportShifts`, `patchStructNewForAddedField`
- `compileOptionalPropertyAccess`, `compileSuperPropertyAccess`, `compileSuperElementAccess`
- `emitLazyProtoGet`, `resolveStructName`, `findExternInfoForMember`
- `isGeneratorIteratorResultLike`, `getIteratorResultValueType`, `getWellKnownSymbolId`

### Re-exports in expressions.ts (backwards-compatible for array-methods.ts, string-ops.ts, index.ts)
- `compilePropertyAccess`, `compileElementAccess`, `emitBoundsCheckedArrayGet`, `emitClampIndex`, `emitClampNonNeg`, `emitNullCheckThrow`, `emitNullGuardedStructGet`, `emitExternrefToStructGet`

### Verification
- Build: esbuild succeeds (3.9MB bundle)
- equivalence tests: identical pass/fail to baseline (1055+ pass / 38 fail, same tests)
- Confirmed 3 test files with pre-existing failures produce identical results on both branches
- `expressions.ts`: 24,090 -> 21,196 lines (-2,894 lines, ~1,494 lines in property-access.ts)
