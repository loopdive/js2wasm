---
priority: high
test262_fail: 1604
feasibility: medium
depends_on: []
---
# #775 -- Null pointer traps should be catchable TypeError (1,604 tests)

## Problem

1,604 tests fail with "dereferencing a null pointer" — a Wasm trap that crashes the module. These should throw a catchable TypeError instead. The previous attempt (#728) was too broad and was reverted.

## Root cause

When property access, method calls, or element access operate on null/undefined values, the Wasm `struct.get` or `ref.as_non_null` instruction traps. Unlike the type-mismatch nulls (where ref.cast fails on wrong struct types), these are genuinely null values that need TypeError throws.

## Fix approach

Insert null guards ONLY on code paths where the value is genuinely null/undefined (not type-mismatch). Key: use `ref.test` before `ref.cast` — if `ref.test` returns false AND `ref.is_null` returns true, it's a real null. If `ref.test` returns false but `ref.is_null` returns false, it's a type mismatch (try alternate structs).

## Acceptance criteria

- Null dereferences throw catchable TypeError
- Type mismatches still return defaults or try alternate structs
- No regressions from the #728 revert
