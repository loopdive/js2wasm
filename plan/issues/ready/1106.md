---
id: 1106
title: "CI baseline-refresh bot wipes plan/ and .claude/memory/ on every run"
sprint: Sprint-42
status: ready
priority: high
feasibility: easy
reasoning_effort: low
depends_on: []
created: 2026-04-12
language_feature: ci
task_type: bug
---

# #1106 — CI baseline-refresh bot wipes plan/ and .claude/memory/ on every run

## Problem

The baseline-refresh bot commits from a stale runner checkout. When tech-lead
commits plan files during the same window, the bot's commit stages deletions of
those files (absent from the runner checkout), wiping them.

Observed: commit `c3452b53` deleted 12 issue files, sprint-42.md, 2 memory
files, pnpm-lock.yaml. Required manual restore (`55a9e41e`).

## Root cause

The promote job uses `git add` without explicit paths (or uses `-am`), and does
not `git pull` before committing.

## Fix

In `.github/workflows/test262-sharded.yml`, the promote/baseline-refresh job:

1. Add `git pull --rebase origin main` immediately before committing
2. Use explicit `git add` for benchmark files only:
   - `benchmarks/results/test262-current.json`
   - `benchmarks/results/test262-current.jsonl`
   - `benchmarks/results/test262-report.json`
   - `benchmarks/results/test262-results.jsonl`
   - `public/benchmarks/results/test262-report.json`
   - `.claude/ci-status/` (the feed files)

## Acceptance criteria

- [ ] Promote job uses explicit `git add <paths>` for benchmark/ci-status files only
- [ ] Promote job runs `git pull --rebase origin main` before committing
- [ ] Concurrent tech-lead commits during CI do NOT cause plan/ deletions
