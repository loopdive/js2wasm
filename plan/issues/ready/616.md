---
priority: medium
test262_ce: 137
feasibility: medium
completed: 2026-03-19
files:
  src/compiler.ts:
    new:
      - "Add diagnostic 2689 to DOWNGRADE_DIAG_CODES"
    breaking: []
depends_on: []
---
# #616 — Cannot extend interface 'Iterator' (137 CE)

## Status: review

137 tests fail with "Cannot extend an interface 'Iterator'. Did you mean 'implements'?" These are Iterator helper method tests (reduce, find, map, etc.) that extend the Iterator protocol.

### Root cause

TypeScript's `Iterator` is declared as an interface. The test262 code tries to `extends Iterator` in a class declaration, which TypeScript rejects (can only extend classes, not interfaces).

### Fix

Either suppress the diagnostic or declare `Iterator` as an abstract class in the lib types so test262 code can extend it.

## Complexity: S

## Implementation Summary

Added TypeScript diagnostic code 2689 ("Cannot extend an interface 'X'. Did you mean 'implements'?") to the `DOWNGRADE_DIAG_CODES` set in `src/compiler.ts`. This downgrades the error to a warning, allowing compilation to proceed for test262 tests that use `class Foo extends Iterator`.

### What was done
- Added code 2689 to `DOWNGRADE_DIAG_CODES` in `src/compiler.ts` (line ~714)

### Files changed
- `src/compiler.ts` — one line addition to DOWNGRADE_DIAG_CODES set

### Verification
- Confirmed that `class MyIterator extends Iterator { ... }` now compiles with a warning instead of an error
- No hard errors remain after the fix
