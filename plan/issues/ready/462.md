---
id: 462
title: "Null check elimination / type narrowing after !== null guards"
status: ready
priority: high
depends_on: []
files:
  src/codegen/expressions.ts:
    breaking:
      - "compileIfStatement / compileConditionalExpression — track narrowed types"
---
# #462 — Null check elimination / type narrowing after !== null guards

After `if (x !== null)`, the true branch should treat `x` as non-null `ref` instead of `ref_null`. Currently redundant `ref.is_null` checks are emitted. Track narrowed types in a scope map.
