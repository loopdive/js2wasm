---
priority: high
feasibility: medium
files:
  tests/test262-vitest.test.ts:
    new:
      - "local HTTP server for wasm source map resolution"
    breaking:
      - "instantiate via fetch URL instead of buffer"
depends_on: []
---
# #725 — Serve wasm binaries via local HTTP for source map stack traces

## Status: open

## Problem

Runtime errors (null pointer deref, illegal cast, unreachable, caught exceptions) show no line numbers because V8 only resolves wasm source maps when loading from a URL, not from an in-memory buffer. This affects ~13,000 failing tests.

Current: `dereferencing a null pointer` (no line info)
Wanted: `dereferencing a null pointer at test.ts:15:3`

## Approach

1. Start a lightweight HTTP server (e.g., `http.createServer`) at test suite startup, serving an output directory
2. Output directory mirrors the test262 input structure:
   - Input: `test262/test/language/expressions/addition/S11.6.1_A1.js`
   - Output: `test262-out/test/language/expressions/addition/S11.6.1_A1.wasm`
   - Source map: `test262-out/test/language/expressions/addition/S11.6.1_A1.wasm.map`
3. Set `sourceMappingURL=S11.6.1_A1.wasm.map` in the wasm binary's custom section (relative to wasm file)
4. Instantiate via `WebAssembly.instantiateStreaming(fetch('http://localhost:PORT/test/language/expressions/addition/S11.6.1_A1.wasm'), imports)`
5. V8 resolves the source map URL relative to the wasm URL, fetches the `.map`, and includes mapped file/line in stack traces
6. Error handlers read `err.stack` which now contains source file:LINE:COL

## Benefits
- All runtime errors get source line numbers automatically
- No manual source map parsing needed (V8 does it)
- No wasm binary bloat (map is separate file)
- Output mirrors input structure — easy to browse, find, and open in browser DevTools
- Works as build cache: if `.wasm` exists and is newer than source, skip recompilation
- Works with existing `resolveWasmErrorLine` as fallback

## Integration with playground (#644)

The same HTTP server serves:
- `/wasm/{hash}.wasm` + `.map` — for V8 source map resolution
- `/report.html` — conformance dashboard
- `/test262/{path}` — test source files (clickable from report)
- `/playground/` — IDE with conformance panel

Browser can also open wasm source maps in DevTools — click a failing test in the report, see the source with breakpoints.

## Complexity: M
