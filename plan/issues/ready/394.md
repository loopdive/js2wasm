---
priority: critical
test262_fail: 629
feasibility: hard
files:
  src/codegen/expressions.ts:
    new: []
    breaking: []
  src/codegen/statements.ts:
    new: []
    breaking: []
depends_on: []
---
# #394 -- Wrong return values in compiled tests (629 FAIL)

## Status: open

629 tests compile successfully but return wrong results at runtime. This is the largest runtime failure bucket and likely represents multiple underlying issues in type coercion, control flow, or missing runtime behavior.

## Details

These tests pass compilation but produce incorrect output. The failures span many test262 categories and likely include:

- Incorrect type coercion (string-to-number, object-to-primitive)
- Control flow bugs (short-circuit evaluation, ternary, switch fallthrough)
- Missing or incorrect operator semantics
- Incorrect handling of edge cases in built-in methods
- Wrong `this` binding in various calling contexts
- Incorrect variable scoping or hoisting

This is an umbrella issue. As specific sub-patterns are identified, they should be split into focused child issues.

## Complexity: L

## Acceptance criteria
- [ ] Identify top 5 sub-patterns causing wrong results
- [ ] Create child issues for each identified pattern
- [ ] Reduce runtime failures by at least 100 tests
