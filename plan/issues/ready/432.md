---
id: 432
title: "new keyword on non-constructor builtins causes stack underflow (42 CE)"
status: ready
priority: medium
depends_on: []
files:
  src/codegen/expressions.ts:
    new: []
    breaking:
      - "compileNewExpression — handle calls to non-constructor functions (Math.abs, etc.)"
      - "compileCallExpression — argument passing for new expressions"
---
# #432 — `new` keyword on non-constructor builtins causes stack underflow (42 CE)

## Problem

42 tests fail with:

```
not enough arguments on the stack for call (need 2, got 0)
```

These are tests that use `new` with non-constructor built-in functions. The tests verify that calling `new Math.abs()` throws a TypeError (because Math methods are not constructors). Instead of throwing TypeError, the compiler generates malformed Wasm that fails to validate.

### Affected categories (42 tests)

- All Math built-in `not-a-constructor.js` tests (33 tests: abs, ceil, floor, round, trunc, sign, sqrt, min, max, clz32, imul, pow, exp, log, sin, cos, tan, asin, acos, atan, atan2, acosh, asinh, atanh, cbrt, expm1, log1p, log2, log10, fround, hypot x2)
- Expression tests using `new` with computed property functions (9 tests: multiplication, division, modulus, bitwise ops, shifts)

### Sample failing tests

- `test/built-ins/Math/abs/not-a-constructor.js`
- `test/language/expressions/multiplication/S11.5.1_A3_T2.2.js`

### Sample test pattern

```javascript
assert.throws(TypeError, () => { new Math.abs(); });
```

## Root cause

When the compiler encounters `new X()`, it tries to emit a constructor call. For built-in Math methods, the compiler does not properly handle the case where the callee is not a constructor. It emits a call instruction that expects constructor arguments (including `this` context) but does not push them onto the stack, resulting in a stack validation error.

## Fix approach

When `new` is used with a known non-constructor (Math methods, arrow functions, etc.), the compiler should either:
1. Emit a TypeError trap directly (since the result is always a TypeError), or
2. Properly handle the `new` expression by checking `[[Construct]]` at runtime

For the expression tests (S11.5.1_A3_T2.2.js pattern), the `new` is applied to objects with `valueOf`/`toString` methods. The compiler needs to handle `new` applied to arbitrary expressions, not just known constructors.

## Priority: medium (42 tests)

## Complexity: M

## Acceptance criteria
- [ ] `new Math.abs()` compiles and throws TypeError at runtime
- [ ] `new` with non-constructor functions does not produce stack validation errors
- [ ] Reduce "not enough arguments on the stack" CEs to zero for this pattern
