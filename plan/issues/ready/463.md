---
id: 463
title: "Self-referencing struct types for linked list / fiber tree patterns"
status: ready
priority: high
depends_on: []
files:
  src/codegen/index.ts:
    breaking:
      - "struct type registration — handle recursive/self-referencing fields"
---
# #463 — Self-referencing struct types for linked list / fiber tree patterns

React's fiber tree uses `child: Fiber | null`, `sibling: Fiber | null`, `return: Fiber | null` — self-referencing nullable struct fields. WasmGC supports this via `rec` groups. Needed for React reconciler milestone 2.
