---
priority: critical
test262_fail: 10988
feasibility: hard
depends_on: []
goal: core-semantics
sprint_role: analysis-only
---
# #779 -- Assert failures: tests compile and run but produce wrong values (10,988 tests)

## Problem

Tests fail with "returned 2" (first assertion failed) or other non-1 return values. The code compiles, instantiates, and runs without crashing — but produces incorrect results. This is the single largest failure category.

### History
- 2026-03-25: 8,700 → 7,096 after fixes #780-#787
- 2026-03-28: 10,988 (count increase from unblocking class/elements, String/prototype, directive-prologue, future-reserved-words tests that were previously skipped)

### Current breakdown by category (2026-03-28, updated from in-progress run)

Total assertion_fail: 6,186 (down from 10,988 -- note: test run still in progress with 31K of 48K tests processed)

| Category | assertion_fail |
|----------|---------------|
| built-ins/Array/prototype | 1,215 |
| language/statements/class | 987 |
| language/expressions/class | 465 |
| language/statements/for-await-of | 386 |
| language/statements/for-of | 332 |
| built-ins/DataView/prototype | 311 |
| language/expressions/object | 234 |
| built-ins/Date/prototype | 210 |
| language/statements/for | 113 |
| language/statements/function | 87 |
| Other | ~1,846 |

### Return code distribution

| Code | Count | Meaning |
|------|-------|---------|
| returned 2 | 4,731 | First assertion failed |
| returned 3 | 722 | Second assertion failed |
| returned 5 | 307 | Fourth assertion failed |
| returned 4 | 185 | Third assertion failed |
| returned 0 | 23 | Early return (tail call optimization tests) |

## Root causes (estimated breakdown)

- ~3,500: Property access returns wrong value (missing property descriptors, wrong struct field)
- ~2,500: Missing built-in method behavior (Array, String, Object methods return wrong results)
- ~2,000: Control flow edge cases (scope, hoisting, TDZ, closures)
- ~1,500: Type coercion errors (toString, valueOf, implicit conversions)
- ~1,500: Other (prototype chain, Symbol, iterator protocol)

## Task: Error analysis and sub-issue creation

This is an **analysis-only** task — no src/ changes. The agent should:

1. Run the test suite (use `TEST262_WORKERS=2 pnpm run test:262`) or read latest results from `benchmarks/results/test262-results.jsonl`
2. Filter for tests with status "fail" and error messages containing "returned" or "assert"
3. Classify the top error patterns by:
   - Error message prefix (e.g., "assert.sameValue", "assert.throws", "returned N")
   - Category (language/expressions, built-ins/Array, etc.)
   - Root cause pattern (sample 10-20 tests per bucket, read the test source to understand what's being tested)
4. Create 5-8 sub-issues in `plan/issues/ready/` numbered #780-#787, each with:
   - Specific error pattern and test count
   - Sample test files
   - Suggested fix approach
   - Files to modify
   - Priority based on test count

## Acceptance criteria

- 5-8 sub-issues created with specific, actionable patterns
- Each sub-issue has ≥5 sample test files
- Total test count across sub-issues accounts for ≥80% of the 8,700
