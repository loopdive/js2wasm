# #166 — Bug: `in` operator runtime failures

## Status: open

## Problem
4 test262 tests fail in `expr/in`. The `in` operator checks property existence on objects. Current implementation may not correctly handle all struct field lookups or may return wrong boolean values.

## Tests blocked
4 test262 failures, 14 compile errors

## Complexity: S
