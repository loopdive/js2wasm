# #167 — Bug: typeof edge cases

## Status: open

## Problem
2 test262 tests fail in `expr/typeof`. The `typeof` operator may not return correct string values for all types (e.g., `typeof null` should be "object", `typeof undefined` should be "undefined").

## Tests blocked
2 test262 failures

## Complexity: XS
