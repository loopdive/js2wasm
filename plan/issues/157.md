# #157 — Bug: void expression returns wrong value

## Status: open

## Problem
1 test262 test fails in `expr/void`. The `void` operator should always return `undefined`, but the emitted code may not produce the correct undefined representation in all contexts.

## Tests blocked
1 test262 failure

## Complexity: XS
