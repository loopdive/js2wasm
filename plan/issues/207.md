# #207 -- Bug: class statement/expression runtime failures (14 fails)

## Status: backlog

## Summary

14 test262 runtime failures across class statements (10) and class expressions (4). These are tests that compile successfully but produce wrong results at runtime.

## Motivation

Failure breakdown:
- `language/statements/class` (10 failures):
  - Class static initializer name binding (2 tests)
  - Class evaluation error ordering (1 test)
  - Other class statement edge cases (7 tests)
- `language/expressions/class` (1 failure):
  - Class expression edge case
- `language/computed-property-names` (1 failure)
- `language/types/reference` (1 failure)

All return 0 instead of expected results. Root cause analysis needed to determine if these share a common codegen bug (likely class field initialization ordering or static initializer scoping).

## Scope
- `src/codegen/statements.ts` -- class declaration codegen
- `src/codegen/expressions.ts` -- class expression codegen

## Complexity
M

## Acceptance criteria
- [ ] Class static initializer name binding works correctly
- [ ] Class evaluation error ordering matches spec
- [ ] 10+ test262 class runtime failures fixed
