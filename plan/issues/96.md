# Issue 96: Test262 coverage — built-ins/JSON

## Summary

Add `built-ins/JSON/parse` and `built-ins/JSON/stringify` to the test262 runner.

## Motivation

JSON is used for data serialization in almost every web application. We support JSON.parse/stringify via host imports.

## Subcategories to add

- `built-ins/JSON/parse`
- `built-ins/JSON/stringify`

## Approach

1. Add paths to `TEST_CATEGORIES`
2. Run and triage — most tests likely need string comparison which goes through externref
3. Focus on what passes with current host import setup

## Complexity

S — small scope, mostly triage

## Dependencies

None
