# #155 — Bug: logical-and/logical-or short-circuit evaluation

## Status: open

## Problem
2 test262 tests fail: `expr/logical-and` (1) and `expr/logical-or` (1). Short-circuit evaluation may not correctly return the last evaluated operand value (JS semantics: `0 && x` returns `0`, not `false`).

## Tests blocked
2 test262 failures

## Complexity: XS
