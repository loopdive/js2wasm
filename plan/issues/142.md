# #142 — Bug: assignment destructuring failures

## Status: open

## Problem
48 test262 tests in `expr/assignment` fail with "returned 0". These are destructuring assignment patterns that compile but produce wrong results. Root causes may include:
- Object rest in destructuring (`{...rest} = obj`) not copying correctly
- Nested destructuring producing wrong bindings
- Destructuring with default values not evaluated properly

## Tests blocked
48 test262 failures in `expr/assignment`

## Complexity: M
