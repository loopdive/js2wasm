# #141 — Bug: tagged template literal runtime failures

## Status: open

## Problem
Tagged template literals compile but produce wrong results (6 failures in `expr/tagged-template`). The tag function likely doesn't receive the correct `strings` array or `raw` property.

Example:
```javascript
function tag(strings, ...values) {
  return strings[0] + values[0];
}
var result = tag`hello ${42}`;
```

## Tests blocked
6 test262 failures in `expr/tagged-template`

## Complexity: S
