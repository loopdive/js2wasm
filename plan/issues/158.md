# #158 — Bug: concatenation with non-string operands

## Status: open

## Problem
1 test262 test fails in `expr/concatenation`. String concatenation with non-string operands (numbers, booleans) may not correctly coerce the operand to string first.

## Tests blocked
1 test262 failure

## Complexity: XS
