# #191 — `assert` not found: tests using raw `assert()` calls

## Status: backlog

## Summary
40 test262 compile errors from "Cannot find name 'assert'". The test262 wrapper replaces `assert(` with `assert_true(` but some assert calls are not being matched, possibly due to whitespace or context differences.

## Motivation
40 test262 compile errors where `assert` is still referenced. The wrapper's regex `assert\s*\(` should match all `assert(` calls, but some cases may involve:
- `assert` as a function reference (not called)
- `assert` in template literals or strings
- Multi-line assert calls where the `(` is on the next line

## Scope
- `tests/test262-runner.ts` — wrapTest() assert replacement logic

## Complexity
S

## Acceptance criteria
- [ ] All `assert` references in test bodies are correctly replaced
- [ ] 40 test262 compile errors fixed
