# #210 -- Bug: for-of destructuring runtime failures (7 fails)

## Status: backlog

## Summary

7 test262 runtime failures in `language/statements/for-of`. These involve object destructuring in for-of loops and TypeError from object-to-primitive conversion.

## Motivation

Failures:
- 5 TypeError "Cannot convert object to primitive value"
- 1 wrong result: `obj-rest-getter-abrupt-get-error.js`
- 1 wrong result: `head-lhs-cover.js`

The TypeError failures suggest the for-of destructuring is trying to convert the iterated object to a primitive when it should be destructuring its properties.

## Scope
- `src/codegen/statements.ts` -- for-of with destructuring patterns
- Object destructuring in loop head

## Complexity
M

## Acceptance criteria
- [ ] `for (const {a, b} of [obj])` correctly destructures without TypeError
- [ ] Object rest in for-of destructuring works
- [ ] 7 test262 for-of failures fixed
