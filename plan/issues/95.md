# Issue 95: Test262 coverage — built-ins/isNaN and built-ins/isFinite

## Summary

Add `built-ins/isNaN` and `built-ins/isFinite` global function tests to the test262 runner.

## Motivation

`isNaN()` and `isFinite()` are commonly used global functions for numeric validation. They have specific coercion semantics that should be validated.

## Subcategories to add

- `built-ins/isNaN`
- `built-ins/isFinite`

## Approach

1. Add paths to `TEST_CATEGORIES`
2. Run and triage
3. May need to compile `isNaN(x)` → `x !== x` and `isFinite(x)` → `x === x && x - x === 0`

## Complexity

S — small number of tests, focused scope

## Dependencies

None
