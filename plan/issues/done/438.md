---
id: 438
title: "Internal error: Cannot read properties of undefined in expression compilation (20 CE)"
status: done
completed: 2026-03-17
priority: medium
test262_ce: 0
complexity: S
depends_on: []
files:
  src/codegen/expressions.ts:
    breaking:
      - "compileExpressionInner -- null safety for undefined property access"
---
# #438 -- Internal error: Cannot read properties of undefined in expression compilation (20 CE)

## Problem

20 tests fail with "Internal error compiling expression: Cannot read properties of undefined (reading ...)" -- a JavaScript TypeError thrown inside the compiler itself during expression compilation.

This indicates the codegen is accessing a property on a value it expects to exist but which is undefined. Common causes:
- Looking up a variable binding that was never registered
- Accessing .type or .kind on an AST node that is unexpectedly null
- Dereferencing a struct type that was not resolved

## Priority: medium (20 tests)

## Complexity: S

## Acceptance criteria
- [x] Identify and fix the specific undefined property accesses
- [x] Add null guards or proper error messages for unresolvable references
- [x] CE count for this internal error pattern reduced to near zero

## Implementation Summary

### What was done

Thorough audit of all 299 `.text` property accesses in `src/codegen/expressions.ts` and 60+ in `src/codegen/statements.ts`. All 82 JSONL entries (covering the 20 CE issue plus duplicates from both strict/sloppy mode runs) had the error pattern "Cannot read properties of undefined (reading 'text')".

### Finding: Already resolved

The 20 CE crashes were **already fixed by prior commits**, specifically:
- Issue #405 (2026-03-16): Added 19 defensive null checks for unsafe `.text` casts
- Various skip filter narrowing commits (eval, delete, new Object(), etc.) that changed which tests were compiled
- Codegen fixes (#444, #447, #448, #445, #441, #436) that improved type handling

### Verification

- Ran test262 on all three originally failing categories (`property-accessors`, `class`, `object`) with **0 "Cannot read properties of undefined" errors**
- Audited all `.text` accesses: every one is properly guarded by `ts.isIdentifier()`, `ts.isStringLiteral()`, `ts.isNumericLiteral()`, or equivalent type checks
- The `compileExpression` try/catch at line 228-242 in expressions.ts provides a safety net for any remaining unhandled crashes
- Equivalence tests pass (element-access-class, element-access-externref, computed-property-class)

### Files reviewed (no changes needed)
- `src/codegen/expressions.ts` -- 299 `.text` accesses, all properly guarded
- `src/codegen/statements.ts` -- 60+ `.text` accesses, all properly guarded
