---
priority: medium
feasibility: medium
completed: 2026-03-19
files:
  src/codegen/expressions.ts:
    new: []
    breaking:
      - "type-specialized arithmetic for known-type operands"
depends_on: []
---
# #597 — Type-specialized arithmetic: skip AnyValue for known types

## Status: review

When both operands of a binary operation have known TypeScript types (both `number`, both `string`), the compiler should emit direct Wasm ops without AnyValue boxing:

```typescript
// x: number, y: number -> direct f64.add (1 instr)
// x: number, y: unknown -> AnyValue dispatch (4-5 calls)
```

Currently some mixed-type paths fall through to AnyValue dispatch even when one side is typed. The coerceType function should short-circuit more aggressively when source types are known.

## Complexity: M

## Implementation Summary

### What was done
Modified the AnyValue dispatch gate in `compileBinaryExpression` to only route through AnyValue helpers (`__any_add`, `__any_sub`, etc.) for operators that genuinely need type-aware dispatch:
- `+` (can be either numeric addition or string concatenation)
- `==`, `!=`, `===`, `!==` (need type-aware equality semantics)

For all other operators (-, *, /, %, **, <, <=, >, >=, &, |, ^, <<, >>, >>>), the code now falls through to compile both operands with a numeric hint (f64), which either:
1. Directly produces f64 values (for numeric literals, typed variables)
2. Unboxes externref to f64 via `__unbox_number` (for any-typed variables stored as externref)

Either way, the result uses direct Wasm arithmetic instructions instead of the AnyValue tagged-union dispatch.

### What worked
- The existing externref unboxing path at the bottom of `compileBinaryExpression` correctly handles the case where any-typed operands compile to externref
- The numeric hint system already handles the conversion from any-typed expressions to f64

### Files changed
- `src/codegen/expressions.ts`: Modified AnyValue dispatch gate (~line 3830)
- `tests/type-specialized-arithmetic.test.ts`: 15 new tests covering both typed and any-typed operands

### Tests now passing
All 15 tests in `type-specialized-arithmetic.test.ts`:
- 5 tests for typed number operands (subtraction, multiplication, division, modulus, comparisons)
- 10 tests for any-typed operands (subtraction, multiplication, division, modulus, comparisons, bitwise, shift, addition, exponentiation, chained)
