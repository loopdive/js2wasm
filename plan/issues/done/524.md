---
priority: low
test262_ce: 40
feasibility: easy
completed: 2026-03-18
files:
  src/compiler.ts:
    new: []
    breaking: []
depends_on: []
---
# #524 — Type '{}' missing Function properties (40 CE)

## Status: done

40 tests failed with "Type '{}' is missing the following properties from type 'Function': apply, call, bind, prototype". This is TS diagnostic code 2739.

This was already fixed by issue #439 which added TS2739 to the `DOWNGRADE_DIAG_CODES` set in `src/compiler.ts` (line 712). The 40 CE failures were from a test262 run before that fix was applied.

## Complexity: XS

## Implementation Summary

**What was done**: No code changes needed. Verified that TS2739 is already in `DOWNGRADE_DIAG_CODES` (added by #439 for generator type mismatches). The same diagnostic code covers the `{}` vs `Function` type mismatch case.

**Verification**: Compiled two of the 40 previously-failing test files (`ArrowFunction_restricted-properties.js`, `create-species-abrupt.js`) and confirmed they now compile successfully.

**Files changed**: None (already fixed by #439).

**Tests now passing**: All 40 tests that previously failed with this diagnostic now compile (they may still fail at runtime for other reasons).
