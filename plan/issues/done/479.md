---
id: 479
title: "Narrow wrapper constructor skip — 155 tests"
status: done
completed: 2026-03-18
priority: medium
depends_on: []
---
# #479 — Narrow wrapper constructor skip (155 tests)

155 tests still skipped for "uses wrapper constructor". Issue #344 made wrapper constructors return primitives, but the skip filter may still be catching some tests. Check if the filter is stale or if these tests genuinely need typeof === "object" behavior.

## Implementation Summary

**Finding:** No stale wrapper constructor skip filter exists. Issue #344 already removed the skip filter completely. The `shouldSkip` function in `tests/test262-runner.ts` contains only a comment at lines 234-235 noting the removal: "Wrapper constructors (new Number, new String, new Boolean) now compile to primitives. No longer skipped."

Verified via test262 results (2026-03-17 run): zero tests are skipped with a "wrapper constructor" reason. Tests with "wrapper" in their filenames that are still skipped are caught by unrelated filters (e.g., "uses Symbol in source", "prototype chain not supported") which are correct and intentional.

**No code changes required.** This issue was already resolved by #344.

**Files inspected:** `tests/test262-runner.ts`
