---
id: 645
title: "Add testTypedArray.js to allowed includes (1,731 skip)"
status: done
priority: medium
completed: 2026-03-19
---

# Add testTypedArray.js to allowed includes

## Problem
1,731 test262 tests are skipped because `testTypedArray.js` is not in the allowed includes set in `shouldSkip()`. TypedArray support was added in #608, so these tests should now be runnable.

## Solution
1. Add `"testTypedArray.js"` to the `allowed` set in `shouldSkip()`
2. Add a `testWithTypedArrayConstructors` stub in `wrapTest()` that iterates over all 8 TypedArray constructors

## Implementation Summary

### What was done
- Added `"testTypedArray.js"` to the allowed includes set in `shouldSkip()` (line ~221)
- Added a `testWithTypedArrayConstructors` preamble stub in `wrapTest()` that calls the provided function with each of the 8 TypedArray constructors: Int8Array, Uint8Array, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array

### Files changed
- `tests/test262-runner.ts`

### Tests
- `tests/abstract-classes.test.ts` passes (6/6)
