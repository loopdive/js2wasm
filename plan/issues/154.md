# #154 — Bug: while/do-while loop condition evaluation

## Status: open

## Problem
4 test262 tests fail in `stmt/while` (2) and `stmt/do-while` (2). Loop condition may not be correctly evaluated for certain expression types (e.g., decrement to zero, complex boolean expressions).

## Tests blocked
4 test262 failures

## Complexity: XS
