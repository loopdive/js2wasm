# #212 -- Bug: object computed property name runtime failures (15 fails)

## Status: backlog

## Summary

15 test262 runtime failures in `language/expressions/object` for computed property names. All tests use computed property names with various expression types and return wrong results.

## Motivation

All 15 failures are `cpn-obj-lit-computed-property-name-from-*` tests that create objects with computed property names using different expression types (additive, assignment, condition, exponentiation, coalesce, logical, identifier, math, multiplicative).

Pattern: `var obj = { [expression]: value }` where the computed key is an expression result. The objects are created but property access via the computed key returns wrong value.

Related to Sprint 1 #140 (partially fixed) -- these are the remaining computed property name failures.

## Scope
- `src/codegen/expressions.ts` -- object literal with computed property names

## Complexity
M

## Acceptance criteria
- [ ] `{ [a + b]: 42 }` creates object with correct computed key
- [ ] All expression types work as computed property keys
- [ ] 15 test262 object cpn failures fixed
