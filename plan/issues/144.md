# #144 — Bug: new expression with class expressions

## Status: open

## Problem
`new` with various expression forms fails. 6 tests in `expr/new` fail with "returned 0". Patterns include:
- `new` with spread arguments: `new Ctor(...args)`
- `new` with complex constructor expressions
- Constructor return value semantics

## Tests blocked
6 test262 failures in `expr/new`, plus 158 compile errors ("Unsupported new expression for class: __class")

## Complexity: M
